---
ntp_packages:
  - {'name': 'ntp', 'state': 'present'}
  - {'name': 'yast2-ntp-client', 'state': 'present'}

ntp_service: ntp

ntp_file_conf: /etc/ntp.conf
ntp_file_drift: /var/lib/ntp/drift/ntp.drift
ntp_dir_stats: /tmp
